# Joxos's .emacs.d (doom ver.)

## Installation

``` sh
git clone https://github.com/hlissner/doom-emacs ~/.emacs.d
~/.emacs.d/bin/doom install
git clone --branch doom https://github.com/Joxos/.emacs.d.git ~/.config/doom/
```

`
